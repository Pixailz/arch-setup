#!/bin/bash -eu

[ "${UID?}" -ne 0 ] && { log "run as root." ; exit 1; }

source ./utils/include

MODE="${1:-"arch-i3"}"

case "${MODE}" in
	"arch-i3")
		arch::install
		i3::install
	;;
esac

exit $?

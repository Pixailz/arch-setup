
function	arch::install()
{
	arch::verify_internet
	arch::load_keymap
}

function	arch::verify_internet()
{ ping -W1 -c1 archlinux.org; }

function	arch::load_keymap()
{
	load_keymap "${ARCH_KEYMAP}"
}
